---
title: "What Svelta Is"
navTitle: "Overview"
description: "svelta is a markdown content engine that can run as docs or blog."
section: "overview"
sectionLabel: "Overview"
order: 1
sectionOrder: 1
updatedAt: "2026-02-22"
tags: ["intro", "architecture"]
---

## Dual-experience model

`svelta` supports two first-class experiences:

- `docs`: hierarchical, section-based, reference-oriented content.
- `blog`: feed-style editorial publishing.

Both run through the same Markdown + mdsvex pipeline.

> [!TIP]
> Use `docs` for structured reference and `blog` for narrative updates. Teams usually need both.

## Why this exists

Most teams start with docs, then add a blog, or the reverse. Svelta keeps both in one package and one authoring workflow.
